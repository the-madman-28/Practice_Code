class Solution {
public:
    static const long long MOD = 1000000007;

    long long power(long long a, long long b) {
        long long ans = 1;

        while (b) {
            if (b & 1)
                ans = ans * a % MOD;

            a = a * a % MOD;
            b >>= 1;
        }

        return ans;
    }

    int prefixStrings(int n) {
        long long comb = 1;

        for (int i = 1; i <= n; i++) {
            comb = comb * (n + i) % MOD;
            comb = comb * power(i, MOD - 2) % MOD;
        }

        return comb * power(n + 1, MOD - 2) % MOD;
    }
};
